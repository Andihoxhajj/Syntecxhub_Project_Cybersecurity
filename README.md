# TCP Port Scanner

A simple TCP port scanner built with Python which:
- Scans open/closed ports
- Uses threading for speed
- Logs results to file
- Handles invalid input gracefully

## Usage

```bash
python3 scanner.py
```


